void setup() {
    size(400, 400);
    background(255);
    fill(0);
    textSize(20);
    text("Hello, World!", 100, 200);
}

void draw() {
    // No need to continuously draw, so we can leave this empty
}